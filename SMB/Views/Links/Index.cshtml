@model IEnumerable<SMB.Models.Links.Subject>
@{
    ViewBag.Title = "Ссылки";
}

<h2>Предметы</h2>
<div class="linkContainer">
        @foreach(var subject in Model)
        {
            <a href="~/Links/Explore/@subject.Id">@subject.Name</a>
        }
</div>

